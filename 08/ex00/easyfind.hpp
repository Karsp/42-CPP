#include <iostream>


template <typename T> 
int* easyfind(T *arr, int key) 
{
	if (!x)
		return ;

	int i = 0;
	for (size_t i = 0; i < arr.size(); ++i)
	{
		if (arr[i] == key)
			return i;
	}

if (index >= _size)
		throw std::out_of_range("Array index out of range");
	
   
}
